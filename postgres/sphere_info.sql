CREATE TABLE sphere_info (
   id serial PRIMARY KEY,
   name VARCHAR(50) NOT NULL,
   diameter VARCHAR(50) NOT NULL,
   weight VARCHAR(50) NOT NULL,
   lat VARCHAR(50) NOT NULL,
   lng VARCHAR(50) NOT NULL,
   angle VARCHAR(50) NOT NULL
);
