window.gameButtons = {
     left: false,
     right: false,
     accelerator: false,
     brakeReverse: false
}

// 30 FPS
function gameLoop() {
    // updatePhysics
    // 
    // redraw
}
setInterval(gameLoop, 33);